# ============= Build output =============
# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# Vite / Vue / TypeScript
node_modules/
dist/
.vite/
.cache/
*.tsbuildinfo
vite.config.ts.timestamp-*

# Vue auto-imports (regenerated by unplugin-auto-import / unplugin-vue-components)
# 注意：types/auto-imports.d.ts 与 types/components.d.ts 是构建产物，但保留进库
# 方便 IDE 直接拿到类型；如团队约定不入库可取消下行注释。
# ontos-rule-web/src/types/auto-imports.d.ts
# ontos-rule-web/src/types/components.d.ts

# 把 web/dist 拷到 backend static 是 fat-jar 构建步骤的产物，不入库
# （每次 npm run build + cp 会重新生成）
ontos-rule-business/src/main/resources/static/

# ============= IDE / Editor =============
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.project
.classpath
.factorypath
*.swp
*~

# ============= OS =============
.DS_Store
Thumbs.db
desktop.ini

# ============= Logs =============
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ============= Local config / secrets =============
.env
.env.local
.env.*.local
*.local
application-local.yml
application-local.properties
application-secret.yml
application-secret.properties

# ============= Runtime data =============
# H2 file DB（DataSeeder 启动时种入；部署后会在仓库根创建持久化卷）
data/
pgdata/
*.mv.db
*.trace.db

# ============= 原型与内部资料（不入库）=============
ontos-rule-prototype/

# ============= Tool metadata =============
# Claude Code / AI assistants 项目级元数据
.claude/
.aider*
.cursor/
